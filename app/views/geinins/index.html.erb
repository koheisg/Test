<main class="content">
    <div class="snackbar"><span><h2>芸人一覧</h2></span></div>
  <div class="lp-timeline">
    <div class="lp-timeline-wrap">
    <% if @geinins.present? %>
	   <% @geinins.each do |geinin| %>
	      <%= render :partial => "followings/following", locals: { geinin: geinin } %>
				<%= link_to geinin.name, "geinins/#{geinin.id}" %>
	   <% end %>
	<% else %>
		<p>芸人の登録はまだありません。</p>
		<p>wArally!の芸人wikiは誰でも登録することができます！
    <%= link_to '芸人を登録する', new_geinin_path %></p>
	<% end %>