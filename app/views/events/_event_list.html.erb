<main class="content">
    <div class="snackbar"><span><h2>ライブ一覧</h2></span></div>
  <div class="lp-timeline">
    <div class="lp-timeline-wrap">
    <% if events.present? %>
	   <% events.each do |event| %>
	      <%= render 'events/event_info', event: event, user: @user %>
	   <% end %>
	<% else %>
		<p>イベントの登録はまだありません。</p>
	<% end %>